#include<stdio.h>
int main()
{
int speed,time,distance;
printf("Enter the values of speed and time=");
scanf("%d %d",&speed,&time);
distance=(speed*time) ;
printf("therefore the distance is=%d",distance);
}
